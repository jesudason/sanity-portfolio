import { Link } from "@mui/material";
import styled from "@emotion/styled";

const StyledLink = styled(Link)`
  text-decoration: none;
  color: #001219;
  font-family: "Karla", sans-serif;
`;

const Section = styled.section`
  margin-bottom: 32px;
`;

export { Section, StyledLink };
